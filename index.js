// Define currentUser 
const currentUser = "Grace Hopper";
// Create welcomeMessage that contains currentUser
const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;
// Create excitedWelcomeMessage (uppercase version of welcomeMessage)
const excitedWelcomeMessage = `WELCOME TO FLATBOOK, ${currentUser.toLocaleUpperCase()}!`;
// Create shortGreeting (uses only the first letter of currentUser)
const shortGreeting = `Welcome, ${currentUser.charAt(0)}!`;